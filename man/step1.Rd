% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step1.R
\name{step1}
\alias{step1}
\title{Step 1: Estimate the Measurement Model}
\usage{
step1(
  data,
  measurementmodel,
  group = NULL,
  invariances = NULL,
  partial_noninvariances = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{measurementmodel}{A string describing the measurement model using the \code{lavaan} syntax.
Can be a list of strings determining the number of measurement blocks (e.g., one string for the MM of factor 1, and a second string for the MM of factor 2).}

\item{group}{String containing the name of the grouping variable. Optional.}

\item{invariances}{String containing the parameters constrained to equality across groups (cf. the \code{group.equal} argument in \code{lavaan})}

\item{partial_noninvariances}{String containing the non-invariances of the model (cf. the \code{group.partial} argument in \code{lavaan}). Must be a list of strings if \code{measurementmodel} is a list (e.g., one string for block 1, and a second string for block 2).}

\item{...}{Arguments to be passed on to \code{\link[lavaan:cfa]{lavaan::cfa()}}.}
}
\value{
A list comprising the output of the measurement model estimation (\code{MMoutput}), the original data set (\code{data}), and the measurement model (\verb{measurement model}).
}
\description{
This function performs step 1 of 3S-LVAR: the estimation of the measurement model. It is a wrapper function around \code{lavaan::cfa()}.
Its output will be passed on to \code{\link[=step2]{step2()}}.
}
